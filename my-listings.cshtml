@model IEnumerable<Homiefy.Models.UserApplicationsView>
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homiefy | My Applications</title>
    <!-- Import Inter Font for modern Neative aesthetic -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/styles.css">
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
    <script>
        if (localStorage.getItem('theme') === 'dark') {
            document.documentElement.setAttribute('data-theme', 'dark');
        }
    </script>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar reveal">
        <div class="nav-container">
            <a href="@Url.Action("Index", "Home")" class="logo">
                <div class="logo-icon"><ion-icon name="home"></ion-icon></div>
                <span>Homiefy</span>
            </a>
            <div class="nav-links">
                <a href="@Url.Action("Index", "Home")">Listings</a>
                <a href="@Url.Action("MyApplications", "Home")" class="active">My Applications</a>
                <a href="@Url.Action("Favorites", "Home")">Favorites</a>
                <a href="@Url.Action("Messages", "Home")">Messages</a>
            </div>
            <div class="nav-actions">
                <button id="themeToggle" class="theme-switch" aria-label="Toggle Theme">
                    <ion-icon name="sunny" class="sun-icon"></ion-icon>
                    <ion-icon name="moon" class="moon-icon"></ion-icon>
                </button>
                <a href="@Url.Action("Profile", "Home")" class="user-profile" style="text-decoration: none;">
                    <span>John Doe</span>
                    <img src="https://api.dicebear.com/7.x/notionists/svg?seed=John" alt="Avatar" class="avatar">
                </a>
                <a href="@Url.Action("Login", "Account")" class="view-apps-btn" style="text-decoration: none;">
                    <ion-icon name="log-out-outline"></ion-icon>
                </a>
            </div>
        </div>
    </nav>

    <main class="container">
        <section class="listings-section reveal delay-1">
            <div class="section-header">
                <h2>Your Applications</h2>
                <div class="filter-chip" id="myApplicationsCount">@(Model != null ? Model.Count() : 0) applications</div>
            </div>
            
            <div class="listings-grid" id="myApplicationsGrid">
                @if (Model != null && Model.Any())
                {
                    int delayCount = 1;
                    foreach (var app in Model)
                    {
                        var delayClass = $"delay-{(delayCount % 3) + 1}";
                        <div class="listing-card reveal @delayClass">
                            <span class="category-badge">Category</span> <!-- Update if category is available in view -->
                            <div style="position: absolute; top: 1.5rem; right: 1.5rem;">
                                <span class="status-badge status-@app.Status.ToLower()" style="font-size: 0.75rem;">@app.Status</span>
                            </div>
                            <div style="padding-top: 1.2rem;">
                                <h3 style="padding-right: 5rem;">@app.Title</h3>
                                <div class="card-location">
                                    <ion-icon name="location"></ion-icon> Location
                                </div>
                                <p>Application to this property is currently @app.Status.</p>
                            </div>
                            
                            <div class="card-foot" style="margin-top: 1rem;">
                                <div class="card-price">
                                    <span class="month">Applied</span>
                                </div>
                                <div style="font-size: 0.8rem; color: var(--text-muted);">Applied: @app.ApplicationDate.GetValueOrDefault().ToString("yyyy-MM-dd")</div>
                            </div>
                        </div>
                        delayCount++;
                    }
                }
                else
                {
                    <div style="grid-column: 1/-1; text-align: center; padding: 4rem 2rem; background: var(--card-bg); border-radius: var(--radius-lg); border: 1px dashed var(--border-color);">
                        <ion-icon name="document-text-outline" style="font-size: 4rem; color: var(--text-muted); margin-bottom: 1rem;"></ion-icon>
                        <h3 style="color: var(--text-primary); font-size: 1.5rem; margin-bottom: 0.5rem;">No applications yet</h3>
                        <p style="color: var(--text-muted);">You haven't applied to any rooms yet. Explore listings to find your new home.</p>
                    </div>
                }
            </div>
        </section>
    </main>

    <script src="~/app.js"></script>
</body>
</html>
